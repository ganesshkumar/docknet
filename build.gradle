buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
    }
}

rootProject.version = '1.0'

apply plugin: 'idea'

allprojects {
    repositories {
        mavenCentral()
        jcenter()
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.12'
}
